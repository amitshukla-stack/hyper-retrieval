# Python
__pycache__/
*.pyc
*.pyo
*.pyd
*.egg-info/
dist/
.venv/
.env

# Logs / temp
*.log
*.jsonl

# Chainlit runtime state
serve/.chainlit/
serve/.files/

# Workspace data (lives in ~/projects/workspaces/, not in this repo)
workspaces/
demo_artifact/
models/
output/
git_history_export.json
git_history_split/
*.lance/
*.npy

# Claude Code context file — local only, never commit
CLAUDE.md

# Org-specific serve files (contain service names/system prompts — local only)
serve/demo_server_v6.py
tools.py

# Org-specific config (contains real employee emails — local only)
config/author_aliases.yaml

*.bak

# Viz data files (org-specific, built locally)
tools/viz/viz_data.json
tools/viz/scatter_data.json
